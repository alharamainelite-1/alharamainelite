import Link from 'next/link';
import { siteConfig } from '@/lib/site';
export default async function RequestSuccess({searchParams}:{searchParams:Promise<{reference?:string;booking?:string;total?:string}>}){const p=await searchParams;const wa=`https://wa.me/${siteConfig.whatsappDigits}?text=${encodeURIComponent('Hello Alharamainelite, I have submitted a journey request through your website and would like to complete my booking.')}`;return <section className="section"><div className="container max-w-3xl"><div className="card p-8 md:p-12"><div className="eyebrow">Request received</div><h1 className="serif mt-4 text-5xl text-forest md:text-6xl">YOUR JOURNEY REQUEST IS COMPLETE.</h1><p className="mt-5 text-lg leading-8 text-forest/65">Your request has been successfully received. Our team will review it and contact you on WhatsApp.</p><div className="mt-8 grid gap-4 bg-[#f2eee3] p-6 sm:grid-cols-3"><div><div className="eyebrow">Reference</div><div className="mt-2 font-semibold text-forest">{p.reference||'HE-XXXXXX'}</div></div><div><div className="eyebrow">Booking</div><div className="mt-2 font-semibold text-forest">{p.booking||'—'}</div></div><div><div className="eyebrow">Estimate</div><div className="mt-2 font-semibold text-forest">{p.total?`$${Number(p.total).toLocaleString()}`:'—'}</div></div></div><h2 className="serif mt-10 text-3xl text-forest">WHAT HAPPENS NEXT?</h2><ol className="mt-5 space-y-4 text-forest/70"><li><b>01</b> — We review your request.</li><li><b>02</b> — Our team contacts you on WhatsApp.</li><li><b>03</b> — We confirm your journey details.</li><li><b>04</b> — We send payment instructions.</li><li><b>05</b> — Your booking is confirmed after payment verification.</li></ol><div className="mt-9 flex flex-wrap gap-3"><a className="btn btn-primary" href={wa} target="_blank" rel="noreferrer">Chat with us on WhatsApp</a><Link className="btn btn-outline" href="/">Return home</Link></div></div></div></section>}
