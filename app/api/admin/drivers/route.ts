import{NextResponse}from"next/server";import{revalidatePath}from"next/cache";import{getCurrentStaff}from"@/lib/supabase/auth";import{getSupabaseAdmin}from"@/lib/supabase/server";const ROLES=["SUPER_ADMIN","ADMIN","OPERATIONS_MANAGER"];
export async function POST(req:Request){const staff=await getCurrentStaff();if(!staff||!ROLES.includes(staff.profile.role))return NextResponse.json({error:"Operations access required."},{status:staff?403:401});const b=await req.json().catch(()=>null)as any;if(!b?.name)return NextResponse.json({error:"Driver name is required."},{status:400});const s=getSupabaseAdmin();const{data,error}=await s.from("drivers").insert({name:String(b.name).slice(0,160),phone:b.phone||null,supplier:b.supplier||null,notes:b.notes||null}).select("*").single();if(error)return NextResponse.json({error:error.message},{status:500});await s.from("audit_logs").insert({actor_id:staff.profile.id,action:"DRIVER_CREATED",entity_type:"driver",entity_id:data.id,after_data:data});revalidatePath("/admin/transportation");return NextResponse.json({driver:data},{status:201});}