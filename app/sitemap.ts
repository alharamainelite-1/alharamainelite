import type {MetadataRoute} from 'next';const routes=['','packages','packages/signature','packages/elite','experience','womens-umrah','makkah','madinah','jeddah','hotels','transportation','about','reviews','faq','request-journey','contact'];export default function sitemap():MetadataRoute.Sitemap{const base=process.env.NEXT_PUBLIC_SITE_URL||'http://localhost:3000';return routes.map(path=>({url:`${base}/${path}`,lastModified:new Date(),changeFrequency:'monthly',priority:path===''?1:.7}))}
