export const dynamic='force-dynamic';
import {redirect} from 'next/navigation';import{getSupabaseAdmin}from'@/lib/supabase/server';import{getCurrentPartner}from'@/lib/supabase/partner';import{PartnerDashboardClient}from'@/components/partner/PartnerDashboardClient';
export default async function PartnerDashboardPage(){const current=await getCurrentPartner();if(!current)redirect('/partner-login');const s=getSupabaseAdmin();const[{data:rows},{data:payouts}]=await Promise.all([s.from('influencer_commissions').select('id,booking_id,type,amount,available_at,status,payout_id,created_at').eq('partner_id',current.partner.id).order('created_at',{ascending:false}).limit(200),s.from('influencer_payouts').select('id,amount,status,rejection_reason,requested_at,paid_at').eq('partner_id',current.partner.id).order('requested_at',{ascending:false}).limit(20)]);const now=Date.now();const ledger=rows||[];const eligible=ledger.filter((x:any)=>x.payout_id===null&&x.status==='PENDING'&&new Date(x.available_at).getTime()<=now);const available=eligible.reduce((n:number,x:any)=>n+(x.type==='RECOVERY'?-Number(x.amount):Number(x.amount)),0);const bookings=new Set(ledger.filter((x:any)=>x.type==='COMMISSION').map((x:any)=>x.booking_id)).size;const sales=ledger.filter((x:any)=>x.type==='COMMISSION').reduce((n:number,x:any)=>n+Number(x.amount)/0.05,0);const paid=(payouts||[]).filter((x:any)=>x.status==='PAID').reduce((n:number,x:any)=>n+Number(x.amount),0);return <main className="min-h-screen bg-ivory"><section className="section"><div className="container max-w-6xl"><div className="flex flex-wrap items-end justify-between gap-5"><div><div className="eyebrow">ALHARAMAIN ELITE Partner Program</div><h1 className="serif mt-3 text-5xl text-forest">Welcome, {current.partner.full_name.split(' ')[0]}.</h1><p className="mt-3 text-forest/55">Your current referral link and partner performance.</p></div><PartnerDashboardClient available={available}/></div><div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><div className="card p-6"><div className="eyebrow">Total Bookings</div><div className="serif mt-2 text-4xl text-forest">{bookings}</div></div><div className="card p-6"><div className="eyebrow">Total Sales</div><div className="serif mt-2 text-4xl text-forest">$ {sales.toLocaleString()}</div></div><div className="card p-6"><div className="eyebrow">Commission Available</div><div className="serif mt-2 text-4xl text-forest">$ {Math.max(0,available).toLocaleString()}</div><p className="mt-2 text-xs text-forest/45">$500 minimum payout</p></div><div className="card p-6"><div className="eyebrow">Commission Paid</div><div className="serif mt-2 text-4xl text-forest">$ {paid.toLocaleString()}</div></div></div><div className="mt-8 grid gap-6 lg:grid-cols-[1fr_360px]"><div className="card p-7"><div className="eyebrow">Your referral link</div><div className="mt-4 break-all rounded-xl bg-[#f7f3ea] p-4 font-semibold text-forest">{process.env.NEXT_PUBLIC_SITE_URL||'https://alharamainelite.vercel.app'}/{current.partner.slug}</div><p className="mt-3 text-sm text-forest/50">Share this link with your audience. Customers receive the normal ALHARAMAIN ELITE website experience.</p></div><div className="card p-7"><div className="eyebrow">Partner status</div><div className="mt-3 inline-flex rounded-full bg-[#f7f3ea] px-4 py-2 text-sm font-semibold text-forest">{current.partner.status}</div><p className="mt-4 text-sm text-forest/50">Your referral link is active while your partner status is Pending or Active.</p></div></div><div className="mt-8 card overflow-hidden"><div className="border-b border-forest/10 p-6"><div className="eyebrow">Recent payout requests</div></div><div className="divide-y divide-forest/10">{(payouts||[]).length===0?<div className="p-8 text-sm text-forest/45">No payout requests yet.</div>:(payouts||[]).map((p:any)=><div key={p.id} className="flex flex-wrap items-center justify-between gap-4 p-5"><div><div className="font-semibold text-forest">$ {Number(p.amount).toLocaleString()}</div><div className="mt-1 text-xs text-forest/45">{new Date(p.requested_at).toLocaleDateString('en-GB')}</div></div><div className="text-right text-sm text-forest/60">{p.status}{p.rejection_reason&&<div className="mt-1 text-xs text-red-700">{p.rejection_reason}</div>}</div></div>)}</div></div></div></section></main>}