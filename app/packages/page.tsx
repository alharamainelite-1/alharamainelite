import Link from 'next/link'; import {packages,features,eliteExtra} from '@/lib/site';
export default function Packages(){return <section className="section"><div className="container"><div className="max-w-3xl"><div className="eyebrow">Our Umrah packages</div><h1 className="serif mt-4 text-6xl text-forest">CLEAR PRICING. THOUGHTFUL JOURNEYS.</h1><p className="mt-5 text-lg leading-8 text-forest/60">Two carefully defined experiences. No hidden package prices.</p></div><div className="mt-12 grid gap-6 md:grid-cols-2">{Object.values(packages).map((p: typeof packages[keyof typeof packages])=><article className="card p-8" key={p.slug}><div className="eyebrow">{p.name}</div><div className="serif mt-3 text-5xl text-forest">${p.price.toLocaleString()}</div><p className="mt-1 text-sm text-forest/50">per guest</p><p className="mt-6 leading-7 text-forest/65">{p.positioning}</p><ul className="mt-6 space-y-3 text-sm text-forest/70">{[...features,...(p.slug==='elite'?eliteExtra:[])].slice(0,8).map(f=><li key={f}>— {f}</li>)}</ul><Link className="btn btn-primary mt-8" href={`/packages/${p.slug}`}>View {p.name}</Link></article>)}</div></div></section>}
