import Link from 'next/link'; import {packages,features,eliteExtra} from '@/lib/site';
export default function Packages(){return <section className="section"><div className="container"><div className="max-w-3xl"><div className="eyebrow">Our Umrah packages</div><h1 className="serif mt-4 text-6xl text-forest">CLEAR PRICING. THOUGHTFUL JOURNEYS.</h1><p className="mt-5 text-lg leading-8 text-forest/60">Two clearly defined 10-day / 9-night experiences for small groups. International flights are not included.</p></div><div className="mt-12 grid gap-6 md:grid-cols-2">{Object.values(packages).map((p: typeof packages[keyof typeof packages])=>{const list=p.slug==='elite'?[...features,...eliteExtra]:features;return <article className="card p-8" key={p.slug}><div className="eyebrow">{p.name}</div><div className="serif mt-3 text-5xl text-forest">{'$'}{p.price.toLocaleString()}</div><p className="mt-1 text-sm text-forest/50">per guest · {p.duration}</p><p className="mt-6 leading-7 text-forest/65">{p.positioning}</p><ul className="mt-6 space-y-3 text-sm text-forest/70">{list.map(f=><li key={f}>— {f}</li>)}</ul><Link className="btn btn-primary mt-8" href={`/packages/${p.slug}`}>View {p.name}</Link></article>})}</div><div className="mt-12 card p-7"><h2 className="serif text-3xl text-forest">A clear request process</h2><p className="mt-3 leading-7 text-forest/65">Select your package and guest count, share your expected travel period, and submit your journey request. Our team will contact you on WhatsApp to confirm details. Payment is handled by bank transfer after the details are confirmed.</p></div></div></section>}