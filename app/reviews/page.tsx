import {cookies} from 'next/headers';import {defaultLocale,isLocale,pageCopy} from '@/lib/i18n';export default async function Reviews(){const raw=(await cookies()).get('he_locale')?.value;const l=isLocale(raw)?raw:defaultLocale;const t=pageCopy[l].reviews;return <section className="section"><div className="container max-w-4xl text-center"><div className="eyebrow">{t.eyebrow}</div><h1 className="serif mt-4 text-6xl text-forest">{t.title}</h1><p className="mx-auto mt-6 max-w-2xl text-lg leading-8 text-forest/60">{t.intro}</p><div className="mx-auto mt-10 max-w-2xl card p-8 text-left"><div className="eyebrow">{t.coming}</div><h2 className="serif mt-3 text-3xl text-forest">{t.comingTitle}</h2><p className="mt-3 text-sm leading-7 text-forest/60">{t.comingText}</p></div></div></section>}