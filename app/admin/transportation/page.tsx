import { getSupabaseAdmin } from '@/lib/supabase/server';
export default async function Page(){let rows:any[]=[];let error='';try{const {data,error:e}=await getSupabaseAdmin().from('vehicles').select('vehicle_id,type,capacity,driver_id,status').limit(100);if(e)throw e;rows=data||[]}catch(e){error=e instanceof Error?e.message:'Unable to load records.'}return <section className="pb-12"><div><div className="eyebrow">Operations</div><h1 className="serif mt-2 text-4xl text-forest">Transportation</h1><p className="mt-2 text-sm text-forest/55">Live records from the Haramain Elite operations system.</p></div>{error&&<div className="mt-6 border border-red-200 bg-red-50 p-4 text-sm text-red-800">Database: {error}</div>}<div className="card mt-6 overflow-x-auto"><table className="w-full min-w-[900px] text-left text-sm"><thead className="border-b border-forest/10 bg-[#faf8f2]"><tr><th key="Vehicle id" className="px-4 py-4 font-semibold text-forest">Vehicle id</th><th key="Type" className="px-4 py-4 font-semibold text-forest">Type</th><th key="Capacity" className="px-4 py-4 font-semibold text-forest">Capacity</th><th key="Driver id" className="px-4 py-4 font-semibold text-forest">Driver id</th><th key="Status" className="px-4 py-4 font-semibold text-forest">Status</th></tr></thead><tbody>{rows.length===0?<tr><td colSpan={5} className="px-4 py-12 text-center text-forest/45">No records yet.</td></tr>:rows.map(r=><tr key={String(r.vehicle_id)} className="border-b border-forest/8"><td key="vehicle_id" className="px-4 py-4">{String(r.vehicle_id ?? '—')}</td><td key="type" className="px-4 py-4">{String(r.type ?? '—')}</td><td key="capacity" className="px-4 py-4">{String(r.capacity ?? '—')}</td><td key="driver_id" className="px-4 py-4">{String(r.driver_id ?? '—')}</td><td key="status" className="px-4 py-4">{String(r.status ?? '—')}</td></tr>)}</tbody></table></div></section>}
