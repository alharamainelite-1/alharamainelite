import { getSupabaseAdmin } from '@/lib/supabase/server';
export default async function Page(){let rows:any[]=[];let error='';try{const {data,error:e}=await getSupabaseAdmin().from('train_bookings').select('booking_id,journey_date,route,passengers,status,reference').limit(100);if(e)throw e;rows=data||[]}catch(e){error=e instanceof Error?e.message:'Unable to load records.'}return <section className="pb-12"><div><div className="eyebrow">Operations</div><h1 className="serif mt-2 text-4xl text-forest">Train bookings</h1><p className="mt-2 text-sm text-forest/55">Live records from the Haramain Elite operations system.</p></div>{error&&<div className="mt-6 border border-red-200 bg-red-50 p-4 text-sm text-red-800">Database: {error}</div>}<div className="card mt-6 overflow-x-auto"><table className="w-full min-w-[900px] text-left text-sm"><thead className="border-b border-forest/10 bg-[#faf8f2]"><tr><th key="Booking id" className="px-4 py-4 font-semibold text-forest">Booking id</th><th key="Journey date" className="px-4 py-4 font-semibold text-forest">Journey date</th><th key="Route" className="px-4 py-4 font-semibold text-forest">Route</th><th key="Passengers" className="px-4 py-4 font-semibold text-forest">Passengers</th><th key="Status" className="px-4 py-4 font-semibold text-forest">Status</th><th key="Reference" className="px-4 py-4 font-semibold text-forest">Reference</th></tr></thead><tbody>{rows.length===0?<tr><td colSpan={6} className="px-4 py-12 text-center text-forest/45">No records yet.</td></tr>:rows.map(r=><tr key={String(r.booking_id)} className="border-b border-forest/8"><td key="booking_id" className="px-4 py-4">{String(r.booking_id ?? '—')}</td><td key="journey_date" className="px-4 py-4">{String(r.journey_date ?? '—')}</td><td key="route" className="px-4 py-4">{String(r.route ?? '—')}</td><td key="passengers" className="px-4 py-4">{String(r.passengers ?? '—')}</td><td key="status" className="px-4 py-4">{String(r.status ?? '—')}</td><td key="reference" className="px-4 py-4">{String(r.reference ?? '—')}</td></tr>)}</tbody></table></div></section>}
