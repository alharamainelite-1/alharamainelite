import { getSupabaseAdmin } from '@/lib/supabase/server';
export default async function Page(){let rows:any[]=[];let error='';try{const {data,error:e}=await getSupabaseAdmin().from('bookings').select('booking_id,guest_count,total_amount,currency,status,payment_status,created_at').limit(100);if(e)throw e;rows=data||[]}catch(e){error=e instanceof Error?e.message:'Unable to load records.'}return <section className="pb-12"><div><div className="eyebrow">Finance</div><h1 className="serif mt-2 text-4xl text-forest">Reports</h1><p className="mt-2 text-sm text-forest/55">Live records from the Haramain Elite operations system.</p></div>{error&&<div className="mt-6 border border-red-200 bg-red-50 p-4 text-sm text-red-800">Database: {error}</div>}<div className="card mt-6 overflow-x-auto"><table className="w-full min-w-[900px] text-left text-sm"><thead className="border-b border-forest/10 bg-[#faf8f2]"><tr><th key="Booking id" className="px-4 py-4 font-semibold text-forest">Booking id</th><th key="Guest count" className="px-4 py-4 font-semibold text-forest">Guest count</th><th key="Total amount" className="px-4 py-4 font-semibold text-forest">Total amount</th><th key="Currency" className="px-4 py-4 font-semibold text-forest">Currency</th><th key="Status" className="px-4 py-4 font-semibold text-forest">Status</th><th key="Payment status" className="px-4 py-4 font-semibold text-forest">Payment status</th><th key="Created at" className="px-4 py-4 font-semibold text-forest">Created at</th></tr></thead><tbody>{rows.length===0?<tr><td colSpan={7} className="px-4 py-12 text-center text-forest/45">No records yet.</td></tr>:rows.map(r=><tr key={String(r.booking_id)} className="border-b border-forest/8"><td key="booking_id" className="px-4 py-4">{String(r.booking_id ?? '—')}</td><td key="guest_count" className="px-4 py-4">{String(r.guest_count ?? '—')}</td><td key="total_amount" className="px-4 py-4">{String(r.total_amount ?? '—')}</td><td key="currency" className="px-4 py-4">{String(r.currency ?? '—')}</td><td key="status" className="px-4 py-4">{String(r.status ?? '—')}</td><td key="payment_status" className="px-4 py-4">{String(r.payment_status ?? '—')}</td><td key="created_at" className="px-4 py-4">{String(r.created_at ?? '—')}</td></tr>)}</tbody></table></div></section>}
