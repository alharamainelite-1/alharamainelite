'use client';
import { FormEvent, useState } from 'react';
import { useRouter } from 'next/navigation';
import { getSupabaseBrowser } from '@/lib/supabase/browser';
export default function AdminLogin(){const r=useRouter();const[email,setEmail]=useState('');const[pw,setPw]=useState('');const[e,setE]=useState('');const[loading,setL]=useState(false);async function submit(x:FormEvent){x.preventDefault();setE('');setL(true);try{const s=getSupabaseBrowser();const {error}=await s.auth.signInWithPassword({email,password:pw});if(error)throw error;r.push('/admin')}catch(err){setE(err instanceof Error?err.message:'Unable to sign in.')}finally{setL(false)}}return <section className="section"><div className="container max-w-md"><div className="card p-8"><div className="eyebrow">Private access</div><h1 className="serif mt-3 text-4xl text-forest">Admin sign in.</h1><form onSubmit={submit} className="mt-8 space-y-5"><label>Email<input required type="email" value={email} onChange={x=>setEmail(x.target.value)}/></label><label>Password<input required type="password" value={pw} onChange={x=>setPw(x.target.value)}/></label>{e&&<p className="text-sm text-red-700">{e}</p>}<button className="btn btn-primary w-full" disabled={loading}>{loading?'Signing in…':'Sign in'}</button></form></div></div></section>}
