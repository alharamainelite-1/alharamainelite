import { getSupabaseAdmin } from '@/lib/supabase/server';
export default async function Page(){let rows:any[]=[];let error='';try{const {data,error:e}=await getSupabaseAdmin().from('site_settings').select('key,value,description,updated_at').limit(100);if(e)throw e;rows=data||[]}catch(e){error=e instanceof Error?e.message:'Unable to load records.'}return <section className="pb-12"><div><div className="eyebrow">Admin</div><h1 className="serif mt-2 text-4xl text-forest">Settings</h1><p className="mt-2 text-sm text-forest/55">Live records from the Haramain Elite operations system.</p></div>{error&&<div className="mt-6 border border-red-200 bg-red-50 p-4 text-sm text-red-800">Database: {error}</div>}<div className="card mt-6 overflow-x-auto"><table className="w-full min-w-[900px] text-left text-sm"><thead className="border-b border-forest/10 bg-[#faf8f2]"><tr><th key="Key" className="px-4 py-4 font-semibold text-forest">Key</th><th key="Value" className="px-4 py-4 font-semibold text-forest">Value</th><th key="Description" className="px-4 py-4 font-semibold text-forest">Description</th><th key="Updated at" className="px-4 py-4 font-semibold text-forest">Updated at</th></tr></thead><tbody>{rows.length===0?<tr><td colSpan={4} className="px-4 py-12 text-center text-forest/45">No records yet.</td></tr>:rows.map(r=><tr key={String(r.key)} className="border-b border-forest/8"><td key="key" className="px-4 py-4">{String(r.key ?? '—')}</td><td key="value" className="px-4 py-4">{String(r.value ?? '—')}</td><td key="description" className="px-4 py-4">{String(r.description ?? '—')}</td><td key="updated_at" className="px-4 py-4">{String(r.updated_at ?? '—')}</td></tr>)}</tbody></table></div></section>}
