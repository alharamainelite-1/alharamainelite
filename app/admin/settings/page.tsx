import { getSupabaseAdmin } from '@/lib/supabase/server';
import { SettingEditor } from '@/components/admin/SettingEditor';
export default async function Page(){let rows:any[]=[];let error='';try{const {data,error:e}=await getSupabaseAdmin().from('site_settings').select('key,value,updated_at').in('key',['group_capacity','languages','tagline','whatsapp']).order('key').limit(100);if(e)throw e;rows=data||[]}catch(e){error=e instanceof Error?e.message:'Unable to load records.'}return <section className="pb-12"><h1 className="serif text-4xl text-forest">Settings</h1><p className="mt-2 text-sm text-forest/55">Operational configuration.</p>{error&&<div className="mt-6 border border-red-200 bg-red-50 p-4">{error}</div>}<div className="card mt-6 overflow-x-auto"><table className="w-full min-w-[700px] text-left text-sm"><thead><tr>{['Key','Value','Updated at','Edit'].map(h=><th key={h} className="px-4 py-4">{h}</th>)}</tr></thead><tbody>{rows.length===0?<tr><td colSpan={4} className="p-12 text-center text-forest/45">No settings configured yet.</td></tr>:rows.map(r=><tr key={String(r.key)}><td className="px-4 py-4 font-semibold">{String(r.key??'—')}</td><td className="px-4 py-4">{String(r.value??'—')}</td><td className="px-4 py-4">{String(r.updated_at??'—')}</td><td className="px-4 py-4"><SettingEditor setting={{key:String(r.key),value:r.value}}/></td></tr>)}</tbody></table></div></section>}