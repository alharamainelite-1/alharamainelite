import { getSupabaseAdmin } from '@/lib/supabase/server';
export default async function Page(){let rows:any[]=[];let error='';try{const {data,error:e}=await getSupabaseAdmin().from('communication_templates').select('id,code,name,language,body,active').limit(100);if(e)throw e;rows=data||[]}catch(e){error=e instanceof Error?e.message:'Unable to load records.'}return <section className="pb-12"><div><div className="eyebrow">Sales</div><h1 className="serif mt-2 text-4xl text-forest">Communication templates</h1><p className="mt-2 text-sm text-forest/55">Live records from the Haramain Elite operations system.</p></div>{error&&<div className="mt-6 border border-red-200 bg-red-50 p-4 text-sm text-red-800">Database: {error}</div>}<div className="card mt-6 overflow-x-auto"><table className="w-full min-w-[900px] text-left text-sm"><thead className="border-b border-forest/10 bg-[#faf8f2]"><tr><th key="Id" className="px-4 py-4 font-semibold text-forest">Id</th><th key="Code" className="px-4 py-4 font-semibold text-forest">Code</th><th key="Name" className="px-4 py-4 font-semibold text-forest">Name</th><th key="Language" className="px-4 py-4 font-semibold text-forest">Language</th><th key="Body" className="px-4 py-4 font-semibold text-forest">Body</th><th key="Active" className="px-4 py-4 font-semibold text-forest">Active</th></tr></thead><tbody>{rows.length===0?<tr><td colSpan={6} className="px-4 py-12 text-center text-forest/45">No records yet.</td></tr>:rows.map(r=><tr key={String(r.id)} className="border-b border-forest/8"><td key="id" className="px-4 py-4">{String(r.id ?? '—')}</td><td key="code" className="px-4 py-4">{String(r.code ?? '—')}</td><td key="name" className="px-4 py-4">{String(r.name ?? '—')}</td><td key="language" className="px-4 py-4">{String(r.language ?? '—')}</td><td key="body" className="px-4 py-4">{String(r.body ?? '—')}</td><td key="active" className="px-4 py-4">{String(r.active ?? '—')}</td></tr>)}</tbody></table></div></section>}
