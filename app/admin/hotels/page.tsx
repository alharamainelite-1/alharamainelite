import { getSupabaseAdmin } from '@/lib/supabase/server';
export default async function Page(){let rows:any[]=[];let error='';try{const {data,error:e}=await getSupabaseAdmin().from('hotels').select('name,city,address,category,room_types,status,notes').limit(100);if(e)throw e;rows=data||[]}catch(e){error=e instanceof Error?e.message:'Unable to load records.'}return <section className="pb-12"><div><div className="eyebrow">Operations</div><h1 className="serif mt-2 text-4xl text-forest">Hotels</h1><p className="mt-2 text-sm text-forest/55">Live records from the Haramain Elite operations system.</p></div>{error&&<div className="mt-6 border border-red-200 bg-red-50 p-4 text-sm text-red-800">Database: {error}</div>}<div className="card mt-6 overflow-x-auto"><table className="w-full min-w-[900px] text-left text-sm"><thead className="border-b border-forest/10 bg-[#faf8f2]"><tr><th key="Name" className="px-4 py-4 font-semibold text-forest">Name</th><th key="City" className="px-4 py-4 font-semibold text-forest">City</th><th key="Address" className="px-4 py-4 font-semibold text-forest">Address</th><th key="Category" className="px-4 py-4 font-semibold text-forest">Category</th><th key="Room types" className="px-4 py-4 font-semibold text-forest">Room types</th><th key="Status" className="px-4 py-4 font-semibold text-forest">Status</th><th key="Notes" className="px-4 py-4 font-semibold text-forest">Notes</th></tr></thead><tbody>{rows.length===0?<tr><td colSpan={7} className="px-4 py-12 text-center text-forest/45">No records yet.</td></tr>:rows.map(r=><tr key={String(r.name)} className="border-b border-forest/8"><td key="name" className="px-4 py-4">{String(r.name ?? '—')}</td><td key="city" className="px-4 py-4">{String(r.city ?? '—')}</td><td key="address" className="px-4 py-4">{String(r.address ?? '—')}</td><td key="category" className="px-4 py-4">{String(r.category ?? '—')}</td><td key="room_types" className="px-4 py-4">{String(r.room_types ?? '—')}</td><td key="status" className="px-4 py-4">{String(r.status ?? '—')}</td><td key="notes" className="px-4 py-4">{String(r.notes ?? '—')}</td></tr>)}</tbody></table></div></section>}
