import { getSupabaseAdmin } from '@/lib/supabase/server';
import { HotelForm } from '@/components/admin/HotelForm';
import {getCurrentStaff} from '@/lib/supabase/auth';
import {getAdminLocale} from '@/lib/admin-locale'; import {adminText} from '@/lib/admin-text';
export default async function Page(){const staff=await getCurrentStaff();if(!staff)return null;const canManage=['SUPER_ADMIN','ADMIN','OPERATIONS_MANAGER'].includes(staff.profile.role);const locale=await getAdminLocale();const t=adminText[locale];let rows:any[]=[];let error='';try{const {data,error:e}=await getSupabaseAdmin().from('hotels').select('id,name,city,address,category,room_types,supplier,contact_name,phone,email,availability_status,notes').order('city').order('name').limit(100);if(e)throw e;rows=data||[]}catch(e){error=e instanceof Error?e.message:'Unable to load hotels.'}return <section className="pb-12">{canManage&&<HotelForm/>}<h1 className="serif text-4xl text-forest">{t.page.hotels}</h1><p className="mt-2 text-sm text-forest/55">{t.common.hotelsDesc}</p>{error&&<div className="mt-6 border border-red-200 bg-red-50 p-4">{error}</div>}<div className="card mt-6 overflow-x-auto"><table className="w-full min-w-[1100px] text-left text-sm"><thead><tr>{(locale==='ar'?['الفندق','المدينة','التصنيف','أنواع الغرف','المورد','جهة الاتصال','التوفر']:['Hotel','City','Category','Room types','Supplier','Contact','Availability']).map(h=><th key={h} className="px-4 py-4">{h}</th>)}</tr></thead><tbody>{rows.length===0?<tr><td colSpan={7} className="p-12 text-center text-forest/45">{t.common.noHotels}</td></tr>:rows.map(r=><tr key={r.id}><td className="px-4 py-4 font-semibold">{r.name}</td><td className="px-4 py-4">{r.city||'—'}</td><td className="px-4 py-4">{r.category||'—'}</td><td className="px-4 py-4">{r.room_types||'—'}</td><td className="px-4 py-4">{r.supplier||'—'}</td><td className="px-4 py-4">{r.contact_name||r.phone||'—'}</td><td className="px-4 py-4">{r.availability_status||'—'}</td></tr>)}</tbody></table></div></section>}