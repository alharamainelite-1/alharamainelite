import{getSupabaseAdmin}from"@/lib/supabase/server";import{getAdminLocale}from"@/lib/admin-locale";import{adminText}from"@/lib/admin-text";
export default async function AuditLogsPage(){const locale=await getAdminLocale();const t=adminText[locale];let rows:any[]=[];let error="";try{const{data,error:e}=await getSupabaseAdmin().from("audit_logs").select("id,action,entity_type,entity_id,created_at,profiles(full_name)").order("created_at",{ascending:false}).limit(200);if(e)throw e;rows=data||[]}catch(e){error=e instanceof Error?e.message:"Unable to load audit logs."}return <section className="pb-12"><div className="eyebrow">{locale==='ar'?'الأمان والتتبع':'Security & traceability'}</div><h1 className="serif mt-2 text-4xl text-forest">{t.page.audit}</h1><p className="mt-2 text-sm text-forest/55">{t.common.auditDesc}</p>{error&&<div className="mt-6 border border-red-200 bg-red-50 p-4">{error}</div>}<div className="card mt-6 overflow-x-auto"><table className="w-full min-w-[900px] text-left text-sm"><thead><tr>{(locale==='ar'?["الوقت","المنفذ","الإجراء","الكيان","معرّف الكيان"]:["Time","Actor","Action","Entity","Entity ID"]).map(h=><th key={h} className="px-4 py-4">{h}</th>)}</tr></thead><tbody>{rows.length===0?<tr><td colSpan={5} className="p-12 text-center text-forest/45">{t.common.noAudit}</td></tr>:rows.map(r=><tr key={String(r.id)} className="border-b border-forest/8 last:border-0"><td className="px-4 py-4 text-xs text-forest/55">{new Date(r.created_at).toLocaleString(locale==='ar'?'ar-SA':'en-GB')}</td><td className="px-4 py-4">{r.profiles?.full_name||(locale==='ar'?"النظام":"System")}</td><td className="px-4 py-4 font-semibold">{r.action}</td><td className="px-4 py-4">{locale==='ar'?({booking:'حجز',journey_request:'طلب رحلة',operations_task:'مهمة تشغيلية',host:'مضيف'} as any)[r.entity_type]||r.entity_type:r.entity_type}</td><td className="px-4 py-4 text-xs">{r.entity_id||"—"}</td></tr>)}</tbody></table></div></section>}