import { getSupabaseAdmin } from '@/lib/supabase/server';
import { HostForm } from '@/components/admin/HostForm';
import { HostStatusForm } from '@/components/admin/HostStatusForm';
import {getAdminLocale} from '@/lib/admin-locale'; import {adminText} from '@/lib/admin-text';
export default async function HostsPage(){const locale=await getAdminLocale();const t=adminText[locale];let rows:any[]=[];let error='';try{const {data,error:e}=await getSupabaseAdmin().from('hosts').select('id,name,phone,whatsapp,city,languages,status,internal_notes').order('name').limit(100);if(e)throw e;rows=data||[]}catch(e){error=e instanceof Error?e.message:'Unable to load hosts.'}return <section className="pb-12"><div><HostForm /></div><div><div className="eyebrow">{t.common.opsManager}</div><h1 className="serif mt-2 text-4xl text-forest">{t.page.hosts}</h1><p className="mt-2 text-sm text-forest/55">{t.common.hostsDesc}</p></div>{error&&<div className="mt-6 border border-red-200 bg-red-50 p-4 text-sm text-red-800">{error}</div>}<div className="card mt-6 overflow-x-auto"><table className="w-full min-w-[900px] text-left text-sm"><thead className="border-b border-forest/10 bg-[#faf8f2]"><tr>{['Host','City','Languages','Phone','WhatsApp','Status','Update'].map(h=><th key={h} className="px-4 py-4 font-semibold text-forest">{h}</th>)}</tr></thead><tbody>{rows.length===0?<tr><td colSpan={6} className="px-4 py-12 text-center text-forest/45">{t.common.noHosts}</td></tr>:rows.map(r=><tr key={r.id} className="border-b border-forest/8"><td className="px-4 py-4 font-semibold">{r.name}</td><td className="px-4 py-4">{r.city||'—'}</td><td className="px-4 py-4">{Array.isArray(r.languages)?r.languages.join(', '):r.languages||'—'}</td><td className="px-4 py-4">{r.phone||'—'}</td><td className="px-4 py-4">{r.whatsapp||'—'}</td><td className="px-4 py-4">{r.status}</td><td className="px-4 py-4"><HostStatusForm id={r.id} current={r.status}/></td></tr>)}</tbody></table></div></section>}
