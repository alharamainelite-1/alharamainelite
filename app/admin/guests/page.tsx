import { getSupabaseAdmin } from '@/lib/supabase/server';
export default async function GuestsPage(){let rows:any[]=[];let error='';try{const {data,error:e}=await getSupabaseAdmin().from('booking_guests').select('id,booking_id,full_name,whatsapp,email,country,city,preferred_language,notes').order('full_name').limit(100);if(e)throw e;rows=data||[]}catch(e){error=e instanceof Error?e.message:'Unable to load guests.'}return <section className="pb-12"><h1 className="serif text-4xl text-forest">Guests</h1><p className="mt-2 text-sm text-forest/55">Guest records linked to journey bookings.</p>{error&&<div className="mt-6 border border-red-200 bg-red-50 p-4">{error}</div>}<div className="card mt-6 overflow-x-auto"><table className="w-full min-w-[850px] text-left text-sm"><thead><tr>{['Name','Booking','WhatsApp','Email','Country','City','Language'].map(h=><th key={h} className="px-4 py-4">{h}</th>)}</tr></thead><tbody>{rows.length===0?<tr><td colSpan={7} className="p-12 text-center text-forest/45">No guest records yet.</td></tr>:rows.map(r=><tr key={r.id}><td className="px-4 py-4 font-semibold">{r.full_name||'—'}</td><td className="px-4 py-4">{r.booking_id||'—'}</td><td className="px-4 py-4">{r.whatsapp||'—'}</td><td className="px-4 py-4">{r.email||'—'}</td><td className="px-4 py-4">{r.country||'—'}</td><td className="px-4 py-4">{r.city||'—'}</td><td className="px-4 py-4">{r.preferred_language||'—'}</td></tr>)}</tbody></table></div></section>}