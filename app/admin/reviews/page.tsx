import { getSupabaseAdmin } from '@/lib/supabase/server';
export default async function Page(){let rows:any[]=[];let error='';try{const {data,error:e}=await getSupabaseAdmin().from('reviews').select('id,guest_name,country,package_id,rating,review_text,review_date,verified,status,created_at').order('created_at',{ascending:false}).limit(100);if(e)throw e;rows=data||[]}catch(e){error=e instanceof Error?e.message:'Unable to load records.'}return <section className="pb-12"><div><div className="eyebrow">Admin</div><h1 className="serif mt-2 text-4xl text-forest">Reviews</h1><p className="mt-2 text-sm text-forest/55">Only verified guest experiences should be published publicly.</p></div>{error&&<div className="mt-6 border border-red-200 bg-red-50 p-4 text-sm text-red-800">{error}</div>}<div className="card mt-6 overflow-x-auto"><table className="w-full min-w-[950px] text-left text-sm"><thead className="border-b border-forest/10 bg-[#faf8f2]"><tr>{['Guest','Country','Rating','Review','Review date','Verified','Status'].map(h=><th key={h} className="px-4 py-4 font-semibold text-forest">{h}</th>)}</tr></thead><tbody>{rows.length===0?<tr><td colSpan={7} className="px-4 py-12 text-center text-forest/45">No review records yet.</td></tr>:rows.map(r=><tr key={String(r.id)} className="border-b border-forest/8"><td className="px-4 py-4 font-semibold">{String(r.guest_name??'—')}</td><td className="px-4 py-4">{String(r.country??'—')}</td><td className="px-4 py-4">{String(r.rating??'—')}</td><td className="px-4 py-4 max-w-md">{String(r.review_text??'—')}</td><td className="px-4 py-4">{String(r.review_date??'—')}</td><td className="px-4 py-4">{r.verified?'Yes':'No'}</td><td className="px-4 py-4">{String(r.status??'—')}</td></tr>)}</tbody></table></div></section>}
