import { getSupabaseAdmin } from '@/lib/supabase/server';
import { ReviewModerationForm } from '@/components/admin/ReviewModerationForm'; import {getAdminLocale} from '@/lib/admin-locale'; import {adminText} from '@/lib/admin-text';
export default async function Page(){const locale=await getAdminLocale();const t=adminText[locale];let rows:any[]=[];let error='';try{const {data,error:e}=await getSupabaseAdmin().from('reviews').select('id,guest_name,country,city,package_id,rating,review_text,review_date,verified,status,created_at').order('created_at',{ascending:false}).limit(100);if(e)throw e;rows=data||[]}catch(e){error=e instanceof Error?e.message:'Unable to load records.'}return <section className="pb-12"><h1 className="serif text-4xl text-forest">{t.page.reviews}</h1><p className="mt-2 text-sm text-forest/55">{t.common.reviewsDesc}</p>{error&&<div className="mt-6 border border-red-200 bg-red-50 p-4">{error}</div>}<div className="card mt-6 overflow-x-auto"><table className="w-full min-w-[900px] text-left text-sm"><thead><tr>{['Guest','City','Country','Rating','Review','Review date','Verified','Status','Moderation'].map(h=><th key={h} className="px-4 py-4">{h}</th>)}</tr></thead><tbody>{rows.length===0?<tr><td colSpan={9} className="p-12 text-center text-forest/45">{t.common.noReviews}</td></tr>:rows.map(r=><tr key={String(r.id)}><td className="px-4 py-4 font-semibold">{String(r.guest_name??'—')}</td><td className="px-4 py-4">{String(r.city??'—')}</td><td className="px-4 py-4">{String(r.country??'—')}</td><td className="px-4 py-4">{String(r.rating??'—')}</td><td className="px-4 py-4 max-w-md">{String(r.review_text??'—')}</td><td className="px-4 py-4">{String(r.review_date??'—')}</td><td className="px-4 py-4">{r.verified?'Yes':'No'}</td><td className="px-4 py-4">{String(r.status??'—')}</td><td className="px-4 py-4"><ReviewModerationForm id={String(r.id)} status={String(r.status??"DRAFT")} verified={!!r.verified}/></td></tr>)}</tbody></table></div></section>}