import { getSupabaseAdmin } from '@/lib/supabase/server';
export default async function Page(){let rows:any[]=[];let error='';try{const {data,error:e}=await getSupabaseAdmin().from('reviews').select('id,customer_name,rating,status,review_text,created_at').limit(100);if(e)throw e;rows=data||[]}catch(e){error=e instanceof Error?e.message:'Unable to load records.'}return <section className="pb-12"><div><div className="eyebrow">Admin</div><h1 className="serif mt-2 text-4xl text-forest">Reviews</h1><p className="mt-2 text-sm text-forest/55">Live records from the Haramain Elite operations system.</p></div>{error&&<div className="mt-6 border border-red-200 bg-red-50 p-4 text-sm text-red-800">Database: {error}</div>}<div className="card mt-6 overflow-x-auto"><table className="w-full min-w-[900px] text-left text-sm"><thead className="border-b border-forest/10 bg-[#faf8f2]"><tr><th key="Id" className="px-4 py-4 font-semibold text-forest">Id</th><th key="Customer name" className="px-4 py-4 font-semibold text-forest">Customer name</th><th key="Rating" className="px-4 py-4 font-semibold text-forest">Rating</th><th key="Status" className="px-4 py-4 font-semibold text-forest">Status</th><th key="Review text" className="px-4 py-4 font-semibold text-forest">Review text</th><th key="Created at" className="px-4 py-4 font-semibold text-forest">Created at</th></tr></thead><tbody>{rows.length===0?<tr><td colSpan={6} className="px-4 py-12 text-center text-forest/45">No records yet.</td></tr>:rows.map(r=><tr key={String(r.id)} className="border-b border-forest/8"><td key="id" className="px-4 py-4">{String(r.id ?? '—')}</td><td key="customer_name" className="px-4 py-4">{String(r.customer_name ?? '—')}</td><td key="rating" className="px-4 py-4">{String(r.rating ?? '—')}</td><td key="status" className="px-4 py-4">{String(r.status ?? '—')}</td><td key="review_text" className="px-4 py-4">{String(r.review_text ?? '—')}</td><td key="created_at" className="px-4 py-4">{String(r.created_at ?? '—')}</td></tr>)}</tbody></table></div></section>}
