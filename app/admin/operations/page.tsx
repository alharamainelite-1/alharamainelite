import { getSupabaseAdmin } from '@/lib/supabase/server';
export default async function OperationsPage(){let rows:any[]=[];let error='';try{const {data,error:e}=await getSupabaseAdmin().from('operations_tasks').select('id,task_id,title,type,status,scheduled_at,group_id,host_id,notes').order('scheduled_at',{ascending:true}).limit(100);if(e)throw e;rows=data||[]}catch(e){error=e instanceof Error?e.message:'Unable to load operations.'}return <section className="pb-12"><div><div className="eyebrow">Operations manager</div><h1 className="serif mt-2 text-4xl text-forest">Operations</h1><p className="mt-2 text-sm text-forest/55">Day-to-day tasks, assignments and journey readiness.</p></div>{error&&<div className="mt-6 border border-red-200 bg-red-50 p-4 text-sm text-red-800">Database: {error}</div>}<div className="card mt-6 overflow-x-auto"><table className="w-full min-w-[950px] text-left text-sm"><thead className="border-b border-forest/10 bg-[#faf8f2]"><tr>{['Task','Type','Scheduled','Group','Host','Status','Notes'].map(h=><th key={h} className="px-4 py-4 font-semibold text-forest">{h}</th>)}</tr></thead><tbody>{rows.length===0?<tr><td colSpan={7} className="px-4 py-12 text-center text-forest/45">No operational tasks yet.</td></tr>:rows.map(r=><tr key={r.id} className="border-b border-forest/8"><td className="px-4 py-4 font-semibold">{r.title||r.task_id||'—'}</td><td className="px-4 py-4">{r.type||'—'}</td><td className="px-4 py-4">{r.scheduled_at?new Date(r.scheduled_at).toLocaleString('en-GB'):'—'}</td><td className="px-4 py-4">{r.group_id||'—'}</td><td className="px-4 py-4">{r.host_id||'Unassigned'}</td><td className="px-4 py-4">{r.status||'—'}</td><td className="px-4 py-4 max-w-xs">{r.notes||'—'}</td></tr>)}</tbody></table></div></section>}
