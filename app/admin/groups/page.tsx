import { getSupabaseAdmin } from '@/lib/supabase/server';
export default async function GroupsPage(){let rows:any[]=[];let error='';try{const {data,error:e}=await getSupabaseAdmin().from('groups').select('id,group_id,name,status,package_slug,departure_period,guest_count,host_id').order('created_at',{ascending:false}).limit(50);if(e)throw e;rows=data||[]}catch(e){error=e instanceof Error?e.message:'Unable to load groups.'}return <section className="pb-12"><div><div className="eyebrow">Operations manager</div><h1 className="serif mt-2 text-4xl text-forest">Groups</h1><p className="mt-2 text-sm text-forest/55">Approved small-group planning and operational assignments.</p></div>{error&&<div className="mt-6 border border-red-200 bg-red-50 p-4 text-sm text-red-800">{error}</div>}<div className="card mt-6 overflow-x-auto"><table className="w-full min-w-[800px] text-left text-sm"><thead className="border-b border-forest/10 bg-[#faf8f2]"><tr>{['Group','Package','Period','Guests','Host','Status'].map(h=><th key={h} className="px-4 py-4 font-semibold text-forest">{h}</th>)}</tr></thead><tbody>{rows.length===0?<tr><td colSpan={6} className="px-4 py-12 text-center text-forest/45">No groups created yet. Groups are created after requests are reviewed and approved.</td></tr>:rows.map(r=><tr key={r.id} className="border-b border-forest/8"><td className="px-4 py-4 font-semibold">{r.group_id||r.name||'—'}</td><td className="px-4 py-4">{r.package_slug||'—'}</td><td className="px-4 py-4">{r.departure_period||'—'}</td><td className="px-4 py-4">{r.guest_count||0}</td><td className="px-4 py-4">{r.host_id?'Assigned':'Unassigned'}</td><td className="px-4 py-4">{r.status||'—'}</td></tr>)}</tbody></table></div></section>}
