import { getSupabaseAdmin } from '@/lib/supabase/server';
export default async function Page(){let rows:any[]=[];let error='';try{const {data,error:e}=await getSupabaseAdmin().from('expenses').select('id,booking_id,group_id,supplier,category,amount,currency,date,reference,notes').order('date',{ascending:false}).limit(100);if(e)throw e;rows=data||[]}catch(e){error=e instanceof Error?e.message:'Unable to load records.'}return <section className="pb-12"><div><div className="eyebrow">Finance</div><h1 className="serif mt-2 text-4xl text-forest">Expenses</h1><p className="mt-2 text-sm text-forest/55">Journey and operational costs linked to bookings or groups.</p></div>{error&&<div className="mt-6 border border-red-200 bg-red-50 p-4 text-sm text-red-800">Database: {error}</div>}<div className="card mt-6 overflow-x-auto"><table className="w-full min-w-[1000px] text-left text-sm"><thead className="border-b border-forest/10 bg-[#faf8f2]"><tr>{['Date','Category','Supplier','Amount','Booking','Group','Reference','Notes'].map(h=><th key={h} className="px-4 py-4 font-semibold text-forest">{h}</th>)}</tr></thead><tbody>{rows.length===0?<tr><td colSpan={8} className="px-4 py-12 text-center text-forest/45">No expense records yet.</td></tr>:rows.map(r=><tr key={String(r.id)} className="border-b border-forest/8"><td className="px-4 py-4">{String(r.date??'—')}</td><td className="px-4 py-4">{String(r.category??'—')}</td><td className="px-4 py-4">{String(r.supplier??'—')}</td><td className="px-4 py-4">{String(r.currency??'—')} {Number(r.amount||0).toLocaleString()}</td><td className="px-4 py-4">{String(r.booking_id??'—')}</td><td className="px-4 py-4">{String(r.group_id??'—')}</td><td className="px-4 py-4">{String(r.reference??'—')}</td><td className="px-4 py-4">{String(r.notes??'—')}</td></tr>)}</tbody></table></div></section>}
