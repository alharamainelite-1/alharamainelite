import { getSupabaseAdmin } from '@/lib/supabase/server';
export default async function Page(){let rows:any[]=[];let error='';try{const {data,error:e}=await getSupabaseAdmin().from('expenses').select('id,category,amount,currency,booking_id,group_id,status,expense_date,notes').limit(100);if(e)throw e;rows=data||[]}catch(e){error=e instanceof Error?e.message:'Unable to load records.'}return <section className="pb-12"><div><div className="eyebrow">Finance</div><h1 className="serif mt-2 text-4xl text-forest">Expenses</h1><p className="mt-2 text-sm text-forest/55">Live records from the Haramain Elite operations system.</p></div>{error&&<div className="mt-6 border border-red-200 bg-red-50 p-4 text-sm text-red-800">Database: {error}</div>}<div className="card mt-6 overflow-x-auto"><table className="w-full min-w-[900px] text-left text-sm"><thead className="border-b border-forest/10 bg-[#faf8f2]"><tr><th key="Id" className="px-4 py-4 font-semibold text-forest">Id</th><th key="Category" className="px-4 py-4 font-semibold text-forest">Category</th><th key="Amount" className="px-4 py-4 font-semibold text-forest">Amount</th><th key="Currency" className="px-4 py-4 font-semibold text-forest">Currency</th><th key="Booking id" className="px-4 py-4 font-semibold text-forest">Booking id</th><th key="Group id" className="px-4 py-4 font-semibold text-forest">Group id</th><th key="Status" className="px-4 py-4 font-semibold text-forest">Status</th><th key="Expense date" className="px-4 py-4 font-semibold text-forest">Expense date</th><th key="Notes" className="px-4 py-4 font-semibold text-forest">Notes</th></tr></thead><tbody>{rows.length===0?<tr><td colSpan={9} className="px-4 py-12 text-center text-forest/45">No records yet.</td></tr>:rows.map(r=><tr key={String(r.id)} className="border-b border-forest/8"><td key="id" className="px-4 py-4">{String(r.id ?? '—')}</td><td key="category" className="px-4 py-4">{String(r.category ?? '—')}</td><td key="amount" className="px-4 py-4">{String(r.amount ?? '—')}</td><td key="currency" className="px-4 py-4">{String(r.currency ?? '—')}</td><td key="booking_id" className="px-4 py-4">{String(r.booking_id ?? '—')}</td><td key="group_id" className="px-4 py-4">{String(r.group_id ?? '—')}</td><td key="status" className="px-4 py-4">{String(r.status ?? '—')}</td><td key="expense_date" className="px-4 py-4">{String(r.expense_date ?? '—')}</td><td key="notes" className="px-4 py-4">{String(r.notes ?? '—')}</td></tr>)}</tbody></table></div></section>}
