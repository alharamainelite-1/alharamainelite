import {cookies} from 'next/headers';import {defaultLocale,isLocale,pageCopy} from '@/lib/i18n';export default async function About(){const raw=(await cookies()).get('he_locale')?.value;const l=isLocale(raw)?raw:defaultLocale;const t=pageCopy[l].about;return <section className="section"><div className="container max-w-5xl"><div className="max-w-4xl"><div className="eyebrow">{t.eyebrow}</div><h1 className="serif mt-4 text-6xl text-forest">{t.title}</h1><p className="mt-6 text-lg leading-8 text-forest/65">{t.intro}</p></div><div className="mt-10 grid gap-4 md:grid-cols-3">{t.items.map(([a,b])=><div className="card p-6" key={a}><h2 className="font-semibold text-forest">{a}</h2><p className="mt-2 text-sm leading-6 text-forest/60">{b}</p></div>)}</div></div></section>}