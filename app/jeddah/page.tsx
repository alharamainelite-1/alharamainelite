import Image from 'next/image';import {cookies} from 'next/headers';import {defaultLocale,isLocale,pageCopy} from '@/lib/i18n';const image='https://images.pexels.com/photos/34744920/pexels-photo-34744920.jpeg?auto=compress&cs=tinysrgb&w=1800';export default async function Jeddah(){const raw=(await cookies()).get('he_locale')?.value;const l=isLocale(raw)?raw:defaultLocale;const t=pageCopy[l].jeddah;return <section className="section"><div className="container max-w-5xl"><div className="relative mb-12 h-72 overflow-hidden rounded-2xl"><Image src={image} alt={t.eyebrow} fill className="object-cover" sizes="100vw"/><div className="absolute inset-0 bg-forest/45"/><div className="absolute bottom-7 left-7 text-white"><div className="eyebrow">{t.eyebrow}</div><h1 className="serif mt-2 text-5xl">{t.title}</h1></div></div><p className="max-w-4xl text-lg leading-8 text-forest/65">{t.intro}</p><div className="mt-10 grid gap-4 md:grid-cols-3">{t.items.map(([a,b])=><div className="card p-6" key={a}><h2 className="font-semibold text-forest">{a}</h2><p className="mt-2 text-sm leading-6 text-forest/60">{b}</p></div>)}</div></div></section>}