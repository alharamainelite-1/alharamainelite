'use client';import{usePathname,useRouter}from'next/navigation';import{useState}from'react';import{Locale,locales}from'@/lib/i18n';export function LanguageSwitcher({locale='en'}:{locale?:Locale}){const router=useRouter(),path=usePathname();const[s,setS]=useState(locale);function change(v:Locale){setS(v);document.cookie='he_locale='+v+';path=/;max-age=31536000;samesite=lax';router.refresh()}return <select aria-label="Language" value={s} onChange={e=>change(e.target.value as Locale)} className="border-0 bg-transparent text-xs font-semibold text-forest focus:ring-0">{locales.map(x=><option key={x} value={x}>{x.toUpperCase()}</option>)}</select>}