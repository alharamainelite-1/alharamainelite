'use client';
import {useState} from 'react';
import type {FormEvent} from 'react';
import {useSearchParams,useRouter} from 'next/navigation';
import {packages,siteConfig} from '@/lib/site';
import {trackEvent} from '@/lib/analytics';
export function JourneyRequestForm(){const params=useSearchParams();const router=useRouter();const initial=params.get('package')==='elite'?'elite':'signature';const rawGuests=Number(params.get('guests')||siteConfig.groupMin);const [pkg,setPkg]=useState<'signature'|'elite'>(initial);const [guests,setGuests]=useState(Math.min(siteConfig.groupMax,Math.max(siteConfig.groupMin,rawGuests)));const[mode,setMode]=useState<'date'|'period'>('period');const[loading,setLoading]=useState(false);const[error,setError]=useState('');const[form,setForm]=useState({fullName:'',whatsapp:'',email:'',country:'',city:'',expectedTravelDate:'',expectedPeriodLabel:'',expectedPeriodStart:'',expectedPeriodEnd:'',preferredLanguage:'en',additionalNotes:''});const total=packages[pkg].price*guests;const update=(key:string,value:string)=>setForm(f=>({...f,[key]:value}));async function submit(e:FormEvent){e.preventDefault();setError('');setLoading(true);trackEvent('request_started',{package:pkg,guests});try{const res=await fetch('/api/journey-requests',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({...form,packageSlug:pkg,guestCount:guests,expectedPeriodLabel:mode==='period'?form.expectedPeriodLabel:'',expectedPeriodStart:mode==='period'?form.expectedPeriodStart:'',expectedPeriodEnd:mode==='period'?form.expectedPeriodEnd:'',expectedTravelDate:mode==='date'?form.expectedTravelDate:''})});const data=await res.json();if(!res.ok){setError(data.error||'Please check your details.');return}trackEvent('request_submitted',{package:pkg,guests,total:data.total});sessionStorage.setItem('he_request',JSON.stringify(data));router.push('/request-success?reference='+encodeURIComponent(data.reference)+'&total='+data.total)}catch{setError('Something went wrong. Please try again.')}finally{setLoading(false)}}return <form onSubmit={submit} className="grid gap-8 lg:grid-cols-[1fr_360px]"><div className="card p-6 md:p-8"><div className="mb-8"><div className="eyebrow">01 · Your journey</div><h2 className="serif mt-2 text-3xl text-forest">Tell us how you would like to travel.</h2><p className="mt-2 text-sm leading-6 text-forest/55">Tell us how many guests are travelling, from 1 to 8, and we will arrange the journey around your group.</p></div><div className="grid gap-6 md:grid-cols-2"><label>Journey<select value={pkg} onChange={e=>setPkg(e.target.value as 'signature'|'elite')}><option value="signature">Signature — $2,000 / guest</option><option value="elite">Elite — $2,500 / guest</option></select></label><label>Number of guests<input required type="number" min={1} max={8} step={1} value={guests} onChange={e=>{const value=Math.min(8,Math.max(1,Number(e.target.value)||1));setGuests(value)}} /></label><label>Full name<input required value={form.fullName} onChange={e=>update('fullName',e.target.value)} autoComplete="name"/></label><label>WhatsApp number<input required value={form.whatsapp} onChange={e=>update('whatsapp',e.target.value)} placeholder="+44…" autoComplete="tel"/></label><label>Email <span className="text-forest/40">(optional)</span><input type="email" value={form.email} onChange={e=>update('email',e.target.value)} autoComplete="email"/></label><label>Country of residence<input required value={form.country} onChange={e=>update('country',e.target.value)} autoComplete="country-name"/></label><label>City <span className="text-forest/40">(optional)</span><input value={form.city} onChange={e=>update('city',e.target.value)}/></label><label>Preferred language<select value={form.preferredLanguage} onChange={e=>update('preferredLanguage',e.target.value)}><option value="en">English</option><option value="so">Somali</option><option value="ar">Arabic</option></select></label></div><fieldset className="mt-10 border-t border-forest/10 pt-8"><legend className="mb-2 font-semibold text-forest">02 · Expected travel period</legend><p className="mb-4 text-sm leading-6 text-forest/55">You do not need a confirmed flight date. Share the period you expect to travel, and our team will coordinate the details with you.</p><div className="flex flex-wrap gap-2"><button type="button" onClick={()=>setMode('period')} className={`btn ${mode==='period'?'btn-primary':'btn-outline'}`}>Approximate period</button><button type="button" onClick={()=>setMode('date')} className={`btn ${mode==='date'?'btn-primary':'btn-outline'}`}>Expected date</button></div></fieldset><div className="mt-5">{mode==='date'?<label>Expected travel date<input type="date" required value={form.expectedTravelDate} onChange={e=>update('expectedTravelDate',e.target.value)}/></label>:<div className="grid gap-5 md:grid-cols-3"><label>Period / month<input required placeholder="October 2026" value={form.expectedPeriodLabel} onChange={e=>update('expectedPeriodLabel',e.target.value)}/></label><label>From <span className="text-forest/40">(optional)</span><input type="date" value={form.expectedPeriodStart} onChange={e=>update('expectedPeriodStart',e.target.value)}/></label><label>To <span className="text-forest/40">(optional)</span><input type="date" value={form.expectedPeriodEnd} onChange={e=>update('expectedPeriodEnd',e.target.value)}/></label></div>}</div><label className="mt-6 block">Additional notes <span className="text-forest/40">(optional)</span><textarea rows={5} value={form.additionalNotes} onChange={e=>update('additionalNotes',e.target.value)} placeholder="Anything you would like our team to know?"/></label>{error&&<div role="alert" className="mt-5 border border-red-200 bg-red-50 p-4 text-sm text-red-800">{error}</div>}<button disabled={loading} className="btn btn-primary mt-7 w-full md:w-auto">{loading?'Submitting…':'Begin my journey request'}</button></div><aside className="card h-fit p-7 lg:sticky lg:top-24"><div className="eyebrow">03 · Your estimate</div><div className="serif mt-3 text-5xl text-forest">${total.toLocaleString()}</div><p className="mt-2 text-sm text-forest/55">{packages[pkg].name} · {guests} guests</p><div className="gold-rule my-6"/><p className="text-sm leading-6 text-forest/60">Estimated total based on your selected journey and group size. International flights are not included.</p><p className="mt-4 text-sm leading-6 text-forest/60">Submitting this form creates a journey request — it does not confirm a booking or payment.</p></aside></form>}
