'use client';import{useState}from'react';const STATUS=['NEW_REQUEST','CONTACTED','DETAILS_PENDING','PAYMENT_PENDING','PAYMENT_RECEIVED','CONFIRMED','PREPARING','ACTIVE','COMPLETED','CANCELLED'] as const;const L:Record<string,string>={NEW_REQUEST:"طلب جديد",CONTACTED:"تم التواصل",DETAILS_PENDING:"بانتظار التفاصيل",PAYMENT_PENDING:"بانتظار الدفع",PAYMENT_RECEIVED:"تم استلام الدفع",CONFIRMED:"مؤكد",PREPARING:"قيد التجهيز",ACTIVE:"نشطة",COMPLETED:"مكتملة",CANCELLED:"ملغاة"};export function BookingStatusForm({bookingId,currentStatus,paymentStatus}:{bookingId:string;currentStatus:string;paymentStatus:string}){const[status,setStatus]=useState(currentStatus),[busy,setBusy]=useState(false),[message,setMessage]=useState('');const ar=typeof document!=="undefined"&&document.cookie.includes("he_locale=ar");async function save(){setBusy(true);setMessage('');const res=await fetch('/api/admin/bookings',{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({bookingId,status})});const data=await res.json().catch(()=>({}));setMessage(res.ok?(ar?'تم الحفظ.':'Saved.'):(data.error||(ar?'تعذر الحفظ.':'Could not save.')));if(res.ok)window.location.reload();setBusy(false)}const canConfirm=paymentStatus==='RECEIVED';return <div className="flex min-w-[270px] items-center gap-2"><select value={status} onChange={e=>setStatus(e.target.value)} className="!py-2 text-xs">{STATUS.map(s=><option key={s} value={s}>{ar?L[s]:s.replaceAll('_',' ')}</option>)}</select><button type="button" disabled={busy||(status==='CONFIRMED'&&!canConfirm)} onClick={save} className="btn btn-outline !px-3 !py-2 text-xs">{busy?(ar?'جارٍ الحفظ…':'Saving…'):(ar?'حفظ':'Save')}</button>{message&&<span className="text-[11px] text-forest/60">{message}</span>}</div>}