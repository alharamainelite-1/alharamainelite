"use client";import{useState}from"react";export function TrainForm({groups}:{groups:{id:string,group_id:string}[]}){const[b,setB]=useState(false),[m,setM]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setB(true);const f=new FormData(e.currentTarget);const r=await fetch("/api/admin/train",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({group_id:f.get("group_id"),travel_date:f.get("travel_date"),origin:f.get("origin"),destination:f.get("destination"),reference:f.get("reference")})});const d=await r.json().catch(()=>({}));setM(r.ok?"Train booking added.":d.error||"Could not add train booking.");setB(false);if(r.ok)location.reload()}return <form onSubmit={submit} className="card mt-6 grid gap-3 p-5 sm:grid-cols-2 lg:grid-cols-5"><select name="group_id" required defaultValue=""><option value="" disabled>Select group</option>{groups.map(g=><option key={g.id} value={g.id}>{g.group_id}</option>)}</select><input name="travel_date" type="date" required/><input name="origin" defaultValue="Makkah" placeholder="Origin"/><input name="destination" defaultValue="Madinah" placeholder="Destination"/><input name="reference" placeholder="Train reference"/><div><button className="btn btn-primary" disabled={b}>{b?"Saving…":"Add train booking"}</button></div>{m&&<span className="text-sm text-forest/60">{m}</span>}</form>}
