'use client';import{useState}from'react';
export function GroupForm({packages,hosts=[],hotels=[]}:{packages:{id:string,name:string}[];hosts?:{id:string,name:string}[];hotels?:{id:string,name:string,city:string}[]}){const ar=typeof document!=='undefined'&&document.cookie.includes('he_locale=ar');const[b,setB]=useState(false),[m,setM]=useState("");
async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setB(true);setM("");const f=new FormData(e.currentTarget);const r=await fetch('/api/admin/groups',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({package_id:f.get('package_id'),capacity:Number(f.get('capacity')),departure_period_start:f.get('start')||null,departure_period_end:f.get('end')||null,host_id:f.get('host_id')||null,hotel_makkah_id:f.get('hotel_makkah_id')||null,hotel_madinah_id:f.get('hotel_madinah_id')||null,hotel_jeddah_id:f.get('hotel_jeddah_id')||null,notes:f.get('notes')||null})});const d=await r.json().catch(()=>({}));setM(r.ok?ar?'تم إنشاء المجموعة.':'Group created.':d.error||ar?'تعذر إنشاء المجموعة.':'Could not create group.');setB(false);if(r.ok)location.reload()}
const city=(c:string)=>hotels.filter(h=>h.city===c);
return <form onSubmit={submit} className="card mt-6 grid gap-3 p-5 sm:grid-cols-2 lg:grid-cols-4"><select name="package_id" required defaultValue=""><option value="" disabled>{ar?'اختر الباقة':'Select package'}</option>{packages.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select><input name="capacity" type="number" min="1" max="8" defaultValue="8"/><input name="start" type="date"/><input name="end" type="date"/><select name="host_id" defaultValue=""><option value="">{ar?'لا يوجد مضيف بعد':'No host yet'}</option>{hosts.map(h=><option key={h.id} value={h.id}>{h.name}</option>)}</select><select name="hotel_makkah_id" defaultValue=""><option value="">{ar?'فندق مكة':'Makkah hotel'}</option>{city("Makkah").map(h=><option key={h.id} value={h.id}>{h.name}</option>)}</select><select name="hotel_madinah_id" defaultValue=""><option value="">{ar?'فندق المدينة':'Madinah hotel'}</option>{city("Madinah").map(h=><option key={h.id} value={h.id}>{h.name}</option>)}</select><select name="hotel_jeddah_id" defaultValue=""><option value="">{ar?'فندق جدة':'Jeddah hotel'}</option>{city("Jeddah").map(h=><option key={h.id} value={h.id}>{h.name}</option>)}</select><input name="notes" placeholder={ar?'ملاحظات':'Notes'} className="sm:col-span-2"/><div><button className="btn btn-primary" disabled={b}>{b?(ar?'جارٍ الحفظ…':'Saving…'):(ar?'إنشاء المجموعة':'Create group')}</button></div>{m&&<span className="text-sm text-forest/60">{m}</span>}</form>}