"use client";import{useState}from"react";export function ExpenseForm(){const[b,setB]=useState(false),[m,setM]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setB(true);const f=new FormData(e.currentTarget);const r=await fetch("/api/admin/expenses",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({amount:f.get("amount"),category:f.get("category"),date:f.get("date"),currency:f.get("currency"),supplier:f.get("supplier"),reference:f.get("reference"),notes:f.get("notes")})});const d=await r.json().catch(()=>({}));setM(r.ok?"Expense added.":d.error||"Could not add expense.");setB(false);if(r.ok)location.reload()}return <form onSubmit={submit} className="card mt-6 grid gap-3 p-5 sm:grid-cols-2 lg:grid-cols-4"><input name="amount" required type="number" min="0.01" step="0.01" placeholder="Amount"/><input name="category" required placeholder="Category"/><input name="date" required type="date"/><select name="currency" defaultValue="USD"><option>USD</option><option>SAR</option></select><input name="supplier" placeholder="Supplier"/><input name="reference" placeholder="Reference"/><input name="notes" placeholder="Notes" className="sm:col-span-2"/><div><button className="btn btn-primary" disabled={b}>{b?"Saving…":"Add expense"}</button></div>{m&&<span className="text-sm text-forest/60">{m}</span>}</form>