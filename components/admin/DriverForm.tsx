"use client";import{useState}from"react";export function DriverForm(){const[b,setB]=useState(false),[m,setM]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setB(true);const f=new FormData(e.currentTarget);const r=await fetch("/api/admin/drivers",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({name:f.get("name"),phone:f.get("phone"),supplier:f.get("supplier"),notes:f.get("notes")})});const d=await r.json().catch(()=>({}));setM(r.ok?"Driver added.":d.error||"Could not add driver.");setB(false);if(r.ok)location.reload()}return <form onSubmit={submit} className="card mt-6 grid gap-3 p-5 sm:grid-cols-2 lg:grid-cols-4"><input name="name" required placeholder="Driver name"/><input name="phone" placeholder="Phone"/><input name="supplier" placeholder="Supplier"/><input name="notes" placeholder="Notes"/><div><button className="btn btn-primary" disabled={b}>{b?"Saving…":"Add driver"}</button></div>{m&&<span className="text-sm text-forest/60">{m}</span>}</form>}