"use client";import{useState}from"react";
const TASK_TYPES=[["AIRPORT_TRANSFER","Airport transfer"],["AIRPORT_ASSISTANCE","Airport assistance"],["HOTEL_TRANSFER","Hotel transfer"],["TRAIN_ASSISTANCE","Train assistance"],["MAKKAH_ZIYARAT","Makkah ziyarat"],["MADINAH_ZIYARAT","Madinah ziyarat"],["JEDDAH_EXPERIENCE","Jeddah experience"],["SPECIAL_ASSISTANCE","Special assistance"],["OTHER","Other"]];
export function OperationsTaskForm(){const ar=typeof document!=='undefined'&&document.cookie.includes('he_locale=ar');const[b,setB]=useState(false),[m,setM]=useState("");
async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setB(true);setM("");const f=new FormData(e.currentTarget);const r=await fetch("/api/admin/operations",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({date:f.get("date"),start_time:f.get("start_time")||null,end_time:f.get("end_time")||null,task_type:f.get("task_type"),location:f.get("location"),notes:f.get("notes")})});const d=await r.json().catch(()=>({}));setM(r.ok?(ar?"تم إنشاء المهمة.":"Task created."):(d.error||(ar?"تعذر إنشاء المهمة.":"Could not create task.")));if(r.ok)e.currentTarget.reset();setB(false);if(r.ok)location.reload()}
return <form onSubmit={submit} className="card mt-6 grid gap-3 p-5 sm:grid-cols-2 lg:grid-cols-4"><input name="date" type="date" required/><input name="start_time" type="time"/><input name="end_time" type="time"/><select name="task_type" required defaultValue=""><option value="" disabled>{ar?'اختر نوع المهمة':'Select task type'}</option>{TASK_TYPES.map(([v,l])=><option key={v} value={v}>{l}</option>)}</select><input name="location" placeholder={ar?'الموقع':'Location'}/><input name="notes" placeholder={ar?'ملاحظات':'Notes'} className="sm:col-span-2"/><div><button className="btn btn-primary" disabled={b}>{b?(ar?"جارٍ الحفظ…":"Saving…"):(ar?"إضافة مهمة":"Add task")}</button></div>{m&&<span className="text-sm text-forest/60">{m}</span>}</form>}