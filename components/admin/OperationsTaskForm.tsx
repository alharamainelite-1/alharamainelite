'use client';import{useState}from'react';export function OperationsTaskForm(){const[b,setB]=useState(false),[m,setM]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setB(true);const f=new FormData(e.currentTarget);const r=await fetch('/api/admin/host-tasks',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({date:f.get('date'),start_time:f.get('start_time')||null,end_time:f.get('end_time')||null,task_type:f.get('task_type'),location:f.get('location'),notes:f.get('notes')})});const d=await r.json().catch(()=>({}));setM(r.ok?'Task created.':d.error||'Could not create task.');if(r.ok)e.currentTarget.reset();setB(false);if(r.ok)location.reload()}return <form onSubmit={submit} className="card mt-6 grid gap-3 p-5 sm:grid-cols-2 lg:grid-cols-4"><input name="date" type="date" required/><input name="start_time" type="time"/><input name="end_time" type="time"/><input name="task_type" required placeholder="Task type"/><input name="location" placeholder="Location"/><input name="notes" placeholder="Notes" className="sm:col-span-2"/><div><button className="btn btn-primary" disabled={b}>{b?'Saving…':'Add task'}</button></div>{m&&<span className="text-sm text-forest/60">{m}</span>}</form>}