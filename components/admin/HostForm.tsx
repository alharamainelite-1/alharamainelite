'use client';
import {useState} from 'react';
export function HostForm(){const ar=typeof document!=='undefined'&&document.cookie.includes('he_locale=ar');const [busy,setBusy]=useState(false),[msg,setMsg]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setMsg('');const f=new FormData(e.currentTarget);const res=await fetch('/api/admin/hosts',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({name:f.get('name'),phone:f.get('phone'),whatsapp:f.get('whatsapp'),city:f.get('city'),languages:String(f.get('languages')||'').split(',').map(x=>x.trim()).filter(Boolean),availability:f.get('availability')})});const d=await res.json().catch(()=>({}));setMsg(res.ok?ar?'تم إنشاء المضيف.':'Host created.':d.error||ar?'تعذر إنشاء المضيف.':'Could not create host.');if(res.ok)e.currentTarget.reset();setBusy(false);if(res.ok)location.reload()}return <form onSubmit={submit} className="card mt-6 grid gap-3 p-5 sm:grid-cols-2 lg:grid-cols-3"><input name="name" required placeholder={ar?'اسم المضيف':'Host name'} /><input name="phone" placeholder={ar?'الهاتف':'Phone'} /><input name="whatsapp" placeholder={ar?'واتساب':'WhatsApp'} /><input name="city" placeholder={ar?'المدينة':'City'} /><input name="languages" placeholder={ar?'اللغات: صومالي، إنجليزي':'Languages: Somali, English'} /><input name="availability" placeholder={ar?'ملاحظات التوفر':'Availability notes'} /><div className="sm:col-span-2 lg:col-span-3 flex items-center gap-3"><button className="btn btn-primary" disabled={busy}>{busy?(ar?'جارٍ الحفظ…':'Saving…'):(ar?'إضافة مضيف':'Add host')}</button>{msg&&<span className="text-sm text-forest/60">{msg}</span>}</div></form>}