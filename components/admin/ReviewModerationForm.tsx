"use client";import{useState}from"react";
export function ReviewModerationForm({id,status,verified}:{id:string;status:string;verified:boolean}){const ar=typeof document!=='undefined'&&document.cookie.includes('he_locale=ar');const[s,setS]=useState(status),[v,setV]=useState(verified),[busy,setBusy]=useState(false),[m,setM]=useState("");
async function save(){setBusy(true);setM("");const r=await fetch("/api/admin/reviews",{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({id,status:s,verified:v})});const d=await r.json().catch(()=>({}));setM(r.ok?(ar?"تم الحفظ.":"Saved."):d.error||(ar?"تعذر الحفظ.":"Could not save."));setBusy(false);if(r.ok)location.reload();}
return <div className="flex min-w-[250px] flex-wrap items-center gap-2"><select value={s} onChange={e=>setS(e.target.value)} className="!py-2 text-xs"><option value="DRAFT">DRAFT</option><option value="PUBLISHED">PUBLISHED</option></select><label className="flex items-center gap-1 text-xs"><input type="checkbox" checked={v} onChange={e=>setV(e.target.checked)}/> ar?'موثق':'Verified'</label><button type="button" disabled={busy} onClick={save} className="btn btn-outline !px-3 !py-2 text-xs">{busy?(ar?"جارٍ الحفظ…":"Saving…"):(ar?"حفظ":"Save")}</button>{m&&<span className="text-[11px] text-forest/60">{m}</span>}</div>}