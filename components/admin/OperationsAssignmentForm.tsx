"use client";import{useState}from"react";export function OperationsAssignmentForm({id,currentStatus}:{id:string;currentStatus:string}){const[s,setS]=useState(currentStatus),[b,setB]=useState(false);async function save(){setB(true);const r=await fetch("/api/admin/operations",{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({id,status:s})});if(r.ok)location.reload();else setB(false)}return <div className="flex gap-2"><select value={s} onChange={e=>setS(e.target.value)} className="!py-2 text-xs">{["PENDING","ASSIGNED","IN_PROGRESS","COMPLETED","CANCELLED"].map(x=><option key={x}>{x}</option>)}</select><button disabled={b} onClick={save} className="btn btn-outline !px-3 !py-2 text-xs">{b?"Saving…":"Save"}</button></div>}