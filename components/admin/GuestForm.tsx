"use client";import{useState}from"react";export function GuestForm({bookings}:{bookings:{id:string,booking_id:string}[]}){const ar=typeof document!=='undefined'&&document.cookie.includes('he_locale=ar');const[b,setB]=useState(false),[m,setM]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setB(true);const f=new FormData(e.currentTarget);const r=await fetch("/api/admin/guests",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({booking_id:f.get("booking_id"),full_name:f.get("full_name"),whatsapp:f.get("whatsapp"),email:f.get("email"),country:f.get("country"),city:f.get("city"),preferred_language:f.get("preferred_language"),notes:f.get("notes")})});const d=await r.json().catch(()=>({}));setM(r.ok?ar?"تمت إضافة العميل.":"Guest added.":d.error||ar?"تعذر إضافة العميل.":"Could not add guest.");setB(false);if(r.ok)location.reload()}return <form onSubmit={submit} className="card mt-6 grid gap-3 p-5 sm:grid-cols-2 lg:grid-cols-4"><select name="booking_id" required defaultValue=""><option value="" disabled>ar?'اختر الحجز':'Select booking'</option>{bookings.map(x=><option key={x.id} value={x.id}>{x.booking_id}</option>)}</select><input name="full_name" required placeholder="ar?'اسم العميل الكامل':'Guest full name'"/><input name="whatsapp" placeholder="WhatsApp"/><input name="email" type="email" placeholder="Email"/><input name="country" placeholder={ar?'الدولة':'Country'}/><input name="city" placeholder={ar?'المدينة':'City'}/><select name="preferred_language" defaultValue="en"><option value="en">English</option><option value="so">Somali</option><option value="ar">Arabic</option></select><input name="notes" placeholder={ar?'ملاحظات':'Notes'}/><div><button className="btn btn-primary" disabled={b}>{b?(ar?"جارٍ الحفظ…":"Saving…"):(ar?"إضافة عميل":"Add guest")}</button></div>{m&&<span className="text-sm text-forest/60">{m}</span>}</form>}
