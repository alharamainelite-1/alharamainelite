"use client";import{useState}from"react";export function RequestStatusForm({id,status}:{id:string;status:string}){const[s,setS]=useState(status),[b,setB]=useState(false);async function save(){setB(true);const r=await fetch("/api/admin/requests",{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({id,status:s})});if(r.ok)location.reload();else setB(false)}return <div className="flex gap-2"><select value={s} onChange={e=>setS(e.target.value)} className="!py-1 text-xs">{["NEW_REQUEST","CONTACTED","DETAILS_PENDING","PAYMENT_PENDING","PAYMENT_RECEIVED","CONFIRMED","PREPARING","ACTIVE","COMPLETED","CANCELLED"].map(x=><option key={x}>{x}</option>)}</select><button disabled={b} onClick={save} className="btn btn-outline !px-2 !py-1 text-xs">{b?"…":"Save"}</button></div>}