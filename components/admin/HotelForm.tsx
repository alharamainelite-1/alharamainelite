"use client";import{useState}from"react";export function HotelForm(){const ar=typeof document!=='undefined'&&document.cookie.includes('he_locale=ar');const[b,setB]=useState(false),[m,setM]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setB(true);const f=new FormData(e.currentTarget);const r=await fetch("/api/admin/hotels",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({name:f.get("name"),city:f.get("city"),category:f.get("category"),address:f.get("address"),supplier:f.get("supplier"),contact_name:f.get("contact_name"),phone:f.get("phone")})});const d=await r.json().catch(()=>({}));setM(r.ok?ar?"تم إنشاء الفندق.":"Hotel created.":d.error||ar?"تعذر إنشاء الفندق.":"Could not create hotel.");setB(false);if(r.ok)location.reload()}return <form onSubmit={submit} className="card mt-6 grid gap-3 p-5 sm:grid-cols-2 lg:grid-cols-4"><input name="name" required placeholder={ar?"اسم الفندق":"Hotel name"}/><input name="city" required placeholder={ar?"المدينة":"City"}/><input name="category" placeholder={ar?"التصنيف / النجوم":"Category / stars"}/><input name="address" placeholder={ar?"العنوان":"Address"}/><input name="supplier" placeholder={ar?"المورد":"Supplier"}/><input name="contact_name" placeholder={ar?"جهة الاتصال":"Contact"}/><input name="phone" placeholder={ar?"الهاتف":"Phone"}/><div><button className="btn btn-primary" disabled={b}>{b?(ar?"جارٍ الحفظ…":"Saving…"):(ar?"إضافة فندق":"Add hotel")}</button></div>{m&&<span className="text-sm text-forest/60">{m}</span>}</form>}
