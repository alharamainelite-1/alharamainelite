"use client";import{useState}from"react";export function HotelForm(){const[b,setB]=useState(false),[m,setM]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setB(true);const f=new FormData(e.currentTarget);const r=await fetch("/api/admin/hotels",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({name:f.get("name"),city:f.get("city"),category:f.get("category"),address:f.get("address"),supplier:f.get("supplier"),contact_name:f.get("contact_name"),phone:f.get("phone")})});const d=await r.json().catch(()=>({}));setM(r.ok?"Hotel created.":d.error||"Could not create hotel.");setB(false);if(r.ok)location.reload()}return <form onSubmit={submit} className="card mt-6 grid gap-3 p-5 sm:grid-cols-2 lg:grid-cols-4"><input name="name" required placeholder="Hotel name"/><input name="city" required placeholder="City"/><input name="category" placeholder="Category / stars"/><input name="address" placeholder="Address"/><input name="supplier" placeholder="Supplier"/><input name="contact_name" placeholder="Contact"/><input name="phone" placeholder="Phone"/><div><button className="btn btn-primary" disabled={b}>{b?"Saving…":"Add hotel"}</button></div>{m&&<span className="text-sm text-forest/60">{m}</span>}</form>}
